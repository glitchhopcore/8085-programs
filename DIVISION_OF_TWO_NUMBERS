MVI A,30 ; DIVIDEND 
MVI B,05 ; DIVISOR
MVI C,00 ; QUOTIENT 
 
L1: CMP B ;IF A < B JUMP TO END
    JC LP1

SUB B  ; A=A-B
INR C ; INCREMENTING QUOTIENT FOR EVERY LOOP 
JNZ L1 ; IF NOT ZERO GO TO LOOP


L2: STA 2000H ; STORING REMAINDER 
    MOV A,C 
    STA 2001H ; STORING QUOTIENT 

HLT	; 
